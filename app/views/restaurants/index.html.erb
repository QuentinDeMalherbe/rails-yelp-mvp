<nav class="navbar navbar-light bg-light">
  <a class="navbar-brand" href="#">C'est trop stylé</a>
  <%= link_to 'Créer un restaurant', new_restaurant_path, class:'navbar-brand' %>
</nav>

<div class="container">
  <h1>Mes restaurants</h1>
  <% @restaurants.each do |restaurant| %>
    <div class="card" style="width: 18rem;">
      <div class="card-body">
        <h5 class="card-title"><%= restaurant.name %></h5>
        <h6 class="card-subtitle mb-2 text-muted"><%= restaurant.address %></h6>
        <h6 class="card-subtitle mb-2 text-muted"><%= restaurant.category %></h6>
        <h6 class="card-subtitle mb-2 text-muted"><%= restaurant.phone_number %></h6>
        <%= link_to 'voir les commentaires', restaurant_path(restaurant), class:'card-link' %>
      </div>
    </div>
  <% end %>
</div>
